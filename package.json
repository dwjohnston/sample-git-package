{
  "name": "sample-git-package",
  "version": "1.0.0",
  "description": "Debugging and issue with Netlify and accessing packages via git",
  
  "type":"module",
  "files": ["dist"],
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
